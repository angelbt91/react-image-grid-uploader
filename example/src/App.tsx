import React from "react";
import ImageGridUploader from "react-image-grid-uploader";

const App = (): JSX.Element => {
  return <ImageGridUploader boxStyles={{ border: "1px solid red" }} name="Angel" />;
};

export default App;
